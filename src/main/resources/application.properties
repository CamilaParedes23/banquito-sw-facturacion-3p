spring.application.name=banquito-switch-billing-service
server.port=${SERVER_PORT:8084}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/SWITCH_COMISIONES_DB}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:db/init/001_create_billing_tables.sql

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

rabbit.exchange.billing=${RABBIT_EXCHANGE_BILLING:switch.billing.exchange}
rabbit.exchange.reporting=${RABBIT_EXCHANGE_REPORTING:switch.reporting.exchange}
rabbit.queue.billing.batch-completed=${RABBIT_QUEUE_BILLING_BATCH_COMPLETED:switch.billing.batch-completed.queue}
rabbit.queue.reporting.billing-completed=${RABBIT_QUEUE_REPORTING_BILLING_COMPLETED:switch.reporting.billing-completed.queue}
rabbit.routing-key.batch-lines-completed=${RABBIT_ROUTING_KEY_BATCH_LINES_COMPLETED:batch.lines.completed}
rabbit.routing-key.billing-completed=${RABBIT_ROUTING_KEY_BILLING_COMPLETED:billing.completed}

switch.billing.fallback-company-ruc=${SWITCH_BILLING_FALLBACK_COMPANY_RUC:1790012345001}
switch.billing.fallback-source-account-number=${SWITCH_BILLING_FALLBACK_SOURCE_ACCOUNT_NUMBER:2200012345}

core.kong.base-url=${CORE_KONG_BASE_URL:http://localhost:8000}
core.kong.switch-core-path=${CORE_KONG_SWITCH_CORE_PATH:/api/v1/switch-core}
core.kong.payment-reservations-path=${CORE_KONG_PAYMENT_RESERVATIONS_PATH:/payment-reservations}
core.kong.auth-token=${CORE_KONG_AUTH_TOKEN:}
core.kong.client-token.enabled=${CORE_KONG_CLIENT_TOKEN_ENABLED:true}
core.kong.client-id=${CORE_KONG_CLIENT_ID:switch-pagos-internos-service}
core.kong.client-secret=${CORE_KONG_CLIENT_SECRET:}
core.kong.required-scope=${CORE_KONG_SERVICE_FEE_REQUIRED_SCOPE:${CORE_KONG_REQUIRED_SCOPE:core.reserve.fee}}
core.kong.client-token-path=${CORE_KONG_CLIENT_TOKEN_PATH:/api/v1/auth/client-token}
core.kong.client-token-refresh-skew-seconds=${CORE_KONG_CLIENT_TOKEN_REFRESH_SKEW_SECONDS:60}
core.kong.connect-timeout-ms=${CORE_KONG_CONNECT_TIMEOUT_MS:5000}
core.kong.read-timeout-ms=${CORE_KONG_READ_TIMEOUT_MS:30000}

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
